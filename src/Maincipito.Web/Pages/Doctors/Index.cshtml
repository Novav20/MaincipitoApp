@page
@model Maincipito.Web.Pages.Doctors.IndexModel

@{
    ViewData["Title"] = "Médicos Registrados";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1>@ViewData["Title"]</h1>
    <a class="btn btn-primary" asp-page="Create">
        <i class="fas fa-user-plus"></i> Registrar nuevo médico
    </a>
</div>

<form method="get" class="mb-4">
    <div class="input-group">
        <input type="text" name="SearchString" value="@Model.SearchString" class="form-control" placeholder="Buscar por nombre, apellido o especialidad..." />
        <div class="input-group-append">
            <button type="submit" class="btn btn-outline-secondary">Buscar</button>
            @if (!string.IsNullOrWhiteSpace(Model.SearchString))
            {
                <a asp-page="./Index" class="btn btn-outline-danger">Limpiar</a>
            }
        </div>
    </div>
</form>

<table class="table table-hover table-striped">
    <thead class="thead-light">
        <tr>
            <th>Nombre(s)</th>
            <th>Apellido(s)</th>
            <th>Especialidad</th>
            <th>Código</th>
            <th>Teléfono</th>
            <th class="text-center">Acciones</th>
        </tr>
    </thead>
    <tbody>
@if (Model.Doctors.Count == 0)
{
    <tr>
        <td colspan="6" class="text-center text-muted">No se encontraron médicos registrados.</td>
    </tr>
}
else
{
    @foreach (var item in Model.Doctors)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.Name)</td>
            <td>@Html.DisplayFor(modelItem => item.Surname)</td>
            <td>@Html.DisplayFor(modelItem => item.MedicalSpecialty)</td>
            <td>@Html.DisplayFor(modelItem => item.Code)</td>
            <td>@Html.DisplayFor(modelItem => item.Cellphone)</td>
            <td class="text-center">
                <a class="btn btn-sm btn-outline-warning" asp-page="./Edit" asp-route-id="@item.Id">Editar</a>
                <a class="btn btn-sm btn-outline-info" asp-page="./Details" asp-route-id="@item.Id">Detalles</a>
                <a class="btn btn-sm btn-outline-danger" asp-page="./Delete" asp-route-id="@item.Id">Borrar</a>
            </td>
        </tr>
    }
}
    </tbody>
</table>