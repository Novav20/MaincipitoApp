@page
@model Maincipito.Web.Pages.Patients.IndexModel

@{
    ViewData["Title"] = "Pacientes Registrados";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1>@ViewData["Title"]</h1>
    <a class="btn btn-primary" asp-page="Create">
        <i class="fas fa-user-plus"></i> Nuevo Paciente
    </a>
</div>

<form method="get" class="mb-4">
    <div class="input-group">
        <input type="text" name="SearchString" value="@Model.SearchString" class="form-control" placeholder="Buscar por nombre o apellido..." />
        <div class="input-group-append">
            <button type="submit" class="btn btn-outline-secondary">Buscar</button>
            @if (!string.IsNullOrWhiteSpace(Model.SearchString))
            {
                <a asp-page="./Index" class="btn btn-outline-danger">Limpiar</a>
            }
        </div>
    </div>
</form>

<table class="table table-hover table-striped">
    <thead class="thead-light">
        <tr>
            <th>ID</th>
            <th>Nombre(s)</th>
            <th>Apellido(s)</th>
            <th>Teléfono</th>
            <th>Dirección</th>
            <th>Ciudad</th>
            <th class="text-center">Acciones</th>
        </tr>
    </thead>
    <tbody>
@if (Model.Patients.Count == 0)
{
    <tr>
        <td colspan="7" class="text-center text-muted">No se encontraron pacientes registrados.</td>
    </tr>
}
else
{
    @foreach (var item in Model.Patients)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td>@item.Surname</td>
            <td>@item.Cellphone</td>
            <td>@item.Address</td>
            <td>@item.City</td>
            <td class="text-center">
                <a class="btn btn-sm btn-outline-warning" asp-page="./Edit" asp-route-id="@item.Id">Editar</a>
                <a class="btn btn-sm btn-outline-info" asp-page="./Details" asp-route-id="@item.Id">Detalles</a>
                <a class="btn btn-sm btn-outline-danger" asp-page="./Delete" asp-route-id="@item.Id">Borrar</a>
            </td>
        </tr>
    }
}
    </tbody>
</table>